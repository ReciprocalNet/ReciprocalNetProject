


VORT2SIMP(1)             USER COMMANDS               VORT2SIMP(1)



NAME
     vort2simp - convert a vort file to a more simple format.

SYNOPSIS
     vort2simp name

DESCRIPTION
     Vort2simp reads in a vort image file and creates two  files,
     one  ending  in ".hdr" which contains header information for
     the file in ascii, and another ending in ".rgb", which  con-
     tains the rgb triples representing the pixels in the image a
     scanline at a time.

SEE ALSO
     art(1), median(1), targ2vort(1), vort2ps(1), gamma(1),  vor-
     tinfo(1), greyscale(1), vort(3), vortfile(5).







































VORT 2.2          Last change: November 9, 1990                 1



