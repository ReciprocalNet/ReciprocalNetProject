


VORT2PS(1)               USER COMMANDS                 VORT2PS(1)



NAME
     vort2ps - convert a VORT file to postscript.

SYNOPSIS
     vort2ps [ -sn ] [ -l ] file1 file2

DESCRIPTION
     Vort2ps reads the pixfile (5) format file file1 and  outputs
     the  postscript  file  file2 for printing on a laser printer
     capable of doing halftones. If the -sn is specified, where n
     is some floating point number, the picture is scaled by n in
     the output. If the -l  is  specified  then  the  picture  is
     printed in landscape mode rather than potrait mode.

SEE ALSO
     art(1),  vortinfo(1),  targ2vort(1),  median(1),   gamma(1),
     greyscale(1), vort(3), vortfile(5).






































VORT 2.2            Last change: Jan 14, 1991                   1



