


TARG2VORT(1)             USER COMMANDS               TARG2VORT(1)



NAME
     targ2vort - convert a targa file to VORT format

SYNOPSIS
     targ2vort [ -s ] file1 file2

DESCRIPTION
     Targ2vort reads the targa file file1 and  outputs  the  VORT
     file  file2 with the 5 bit targa RGB values shifted up by 3.
     If the -s is specified the colour shifting is not done.

SEE ALSO
     art(1), vortinfo(1), median(1), vort2ps(1), gamma(1), greys-
     cale(1), vortfile(5), vort(3).









































VORT 2.2            Last change: June 3, 1989                   1



