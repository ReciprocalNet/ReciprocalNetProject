


GAMMA(1)                 USER COMMANDS                   GAMMA(1)



NAME
     gamma - perform gamma correction on data in a pix file

SYNOPSIS
     gamma [ -g n ] file1 file2

DESCRIPTION
     Gamma applys gamma correction to an  VORT  image  stored  in
     file1 to generate an VORT file in file2.

SEE ALSO
     art(1), vortinfo(1),  targ2vort(1),  vort2ps(1),  median(1),
     greyscale(1), vortfile(5).  vort(3).










































VORT 2.2          Last change: August 24, 1989                  1



